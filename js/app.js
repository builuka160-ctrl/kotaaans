const copy = {
  ru: {
    'nav.services':'Услуги','nav.work':'Работы','nav.visit':'Контакты','nav.book':'Записаться',
    'hero.kicker':'БАРБЕРШОП · РЕЗЕКНЕ','hero.title':'Точная форма.<br /><em>Время для себя.</em>','hero.lead':'Внимательная работа, спокойная атмосфера и время, которое действительно выделено вам. Только по записи.','hero.cta':'Записаться <span>↘</span>','hero.about':'О барбершопе <span>↓</span>','hero.caption1':'Dārzu iela 22','hero.caption2':'По записи','hero.scroll':'ЛИСТАЙТЕ ДАЛЬШЕ',
    'about.kicker':'ПОДХОД KOTANS','about.title':'Ухоженность — это <em>тихая уверенность.</em>','about.copy':'Kotans — для тех, кто ценит точный результат и спокойную атмосферу. Перед началом мы учитываем особенности волос, бороды и привычный вам стиль.','about.copy2':'В команде работают квалифицированные барберы. Техника, гигиена и внимательный подход к каждому гостю — основа нашей работы.','facts.one':'Личная<br />консультация','facts.two':'Ритм записи<br />30 минут','facts.three':'Резекне,<br />Латвия',
    'services.kicker':'УСЛУГИ','services.title':'Создано под <em>ваш образ.</em>','services.note':'Выберите услугу и удобную дату — система сама назначит ближайшее свободное время.','services.consult':'консультация','services.ask':'уточнить у барбера','service.haircut.name':'Стрижка','service.haircut.text':'Точная стрижка с короткой консультацией перед началом работы.','service.beard.name':'Стрижка бороды','service.beard.text':'Форма, чистые линии и уход с учётом естественного роста бороды.','service.combo.name':'Стрижка + борода','service.combo.text':'Полная запись для цельного, аккуратного образа.','service.hairTone.name':'Тонировка волос','service.hairTone.text':'Тонировка подбирается индивидуально: обсудите с барбером результат и стоимость.','service.beardTone.name':'Тонировка бороды','service.beardTone.text':'Естественное обновление оттенка, согласованное с барбером перед визитом.',
    'work.kicker':'В КРЕСЛЕ','work.title':'Работа говорит <em>сама за себя.</em>','work.one':'Внимание к каждому движению.','work.two':'Финал, созданный для вас.',
    'booking.kicker':'ОНЛАЙН-ЗАПИСЬ','booking.title':'Ваше кресло <em>ждёт.</em>','booking.copy':'Укажите данные, выберите услугу и дату. Система забронирует первое подходящее время в расписании этого дня.','booking.notify':'Включить уведомления о записи','form.name':'Имя и фамилия','form.age':'Возраст','form.service':'Услуга','form.date':'Желаемая дата','form.submit':'Оставить заявку','form.note':'Время выбирать не нужно: заявке назначается первое свободное время. Затем администратор подтвердит её.','form.open':'В этот день работаем с {open} до {close}. Вам будет назначено ближайшее свободное время.','form.closed':'В этот день барбершоп не работает. Выберите другую дату.','form.booked':'Заявка принята: {date} в {time}. После проверки администратор подтвердит запись.','form.noSlot':'На этот день свободного времени уже нет. Выберите другую дату.','form.error':'Не получилось отправить заявку. Проверьте данные и попробуйте ещё раз.','form.notifyOn':'Уведомления включены.','form.notifyOff':'Уведомления недоступны или не разрешены в этом браузере.','form.available':'Свободно','form.full':'Нет мест','form.loading':'Проверяем',
    'visit.kicker':'КАК НАС НАЙТИ','visit.title':'Приходите,<br /><em>занимайте место.</em>','visit.addressLabel':'Адрес','visit.phoneLabel':'Телефон','visit.hoursLabel':'Часы работы','visit.hours':'Время доступно<br />в онлайн-расписании','footer.copy':'Квалифицированные барберы. Продуманный уход. Резекне.','footer.admin':'Администратор',
    'admin.kicker':'ЗАКРЫТАЯ ЗОНА','admin.loginTitle':'Вход для администратора','admin.password':'Пароль','admin.enter':'Войти','admin.title':'Заявки','admin.notifications':'Включить уведомления','admin.logout':'Выйти','admin.scheduleKicker':'РАБОЧЕЕ ВРЕМЯ','admin.scheduleTitle':'Расписание на неделю','admin.scheduleCopy':'Посетителям будет назначаться ближайшее время в этих пределах. Отметьте день выходным, если не работаете.','admin.saveHours':'Сохранить расписание','admin.blockedKicker':'КОНТРОЛЬ ДОСТУПА','admin.blockedTitle':'Заблокированные IP-адреса','admin.loginError':'Неверный пароль.','admin.loginGeneric':'Не удалось войти. Попробуйте ещё раз.','admin.empty':'Заявок пока нет.','admin.pending':'ожидает решения','admin.accepted':'подтверждена','admin.rejected':'отклонена','admin.accept':'Принять','admin.reject':'Отклонить','admin.delete':'Удалить','admin.block':'Заблокировать IP','admin.unblock':'Разблокировать','admin.reason':'Напишите причину отказа для клиента:','admin.deleteConfirm':'Удалить эту заявку окончательно? Она исчезнет и с сайта, и из Google Sheets.','admin.blockConfirm':'Заблокировать этот IP-адрес? Новые заявки с него больше не будут приниматься.','admin.hoursSaved':'Расписание сохранено.','admin.actionError':'Не удалось выполнить действие. Обновите страницу и попробуйте снова.','admin.newCount':'Всего: {all} · ожидают: {pending}','admin.notificationsOn':'Уведомления администратора включены.','admin.day0':'Воскресенье','admin.day1':'Понедельник','admin.day2':'Вторник','admin.day3':'Среда','admin.day4':'Четверг','admin.day5':'Пятница','admin.day6':'Суббота','admin.open':'Начало','admin.close':'Конец','admin.closed':'Выходной','admin.reasonNeeded':'Причина обязательна.','theme.light':'Светлая','theme.dark':'Тёмная'
  },
  lv: {
    'nav.services':'Pakalpojumi','nav.work':'Darbi','nav.visit':'Kontakti','nav.book':'Pieteikt vizīti',
    'hero.kicker':'BĀRBERU SALONS · RĒZEKNE','hero.title':'Precīzs griezums.<br /><em>Laiks sev.</em>','hero.lead':'Rūpīgs darbs, mierīga vide un laiks, kas patiešām ir veltīts tev. Tikai pēc iepriekšēja pieraksta.','hero.cta':'Pieteikt vizīti <span>↘</span>','hero.about':'Par salonu <span>↓</span>','hero.caption1':'Dārzu iela 22','hero.caption2':'Tikai ar pierakstu','hero.scroll':'RITINIET TĀLĀK',
    'about.kicker':'KOTANS PIEEJA','about.title':'Kopts izskats ir <em>klusa pārliecība.</em>','about.copy':'Kotans ir vieta tiem, kuri novērtē precīzu rezultātu un mierīgu atmosfēru. Pirms darba sākuma ņemam vērā matus, bārdu un tavu ikdienas stilu.','about.copy2':'Mūsu komandā strādā kvalificēti bārddziņi. Tehnika, higiēna un vērīga attieksme pret katru klientu ir mūsu darba pamats.','facts.one':'Personiska<br />konsultācija','facts.two':'Pieraksta ritms<br />30 minūtes','facts.three':'Rēzekne,<br />Latvija',
    'services.kicker':'PAKALPOJUMI','services.title':'Pielāgots <em>tavam tēlam.</em>','services.note':'Izvēlies pakalpojumu un vēlamo datumu — sistēma automātiski piešķirs tuvāko brīvo laiku.','services.consult':'konsultācija','services.ask':'jautājiet bārddzinim','service.haircut.name':'Matu griezums','service.haircut.text':'Precīzs, tev pielāgots griezums ar īsu konsultāciju pirms darba.','service.beard.name':'Bārdas griezums','service.beard.text':'Forma, tīras līnijas un kopšana, kas respektē dabisko bārdas augšanu.','service.combo.name':'Mati + bārda','service.combo.text':'Pilna vizīte saskaņotam un rūpīgi veidotam tēlam.','service.hairTone.name':'Matu tonēšana','service.hairTone.text':'Tonēšana tiek plānota individuāli — pārrunā ar bārddzini vēlamo rezultātu un cenu.','service.beardTone.name':'Bārdas tonēšana','service.beardTone.text':'Dabisks toņa atsvaidzinājums, ko pirms vizītes saskaņo ar bārddzini.',
    'work.kicker':'KRĒSLĀ','work.title':'Darbs runā <em>pats par sevi.</em>','work.one':'Precizitāte katrā kustībā.','work.two':'Noslēgums, kas radīts tev.',
    'booking.kicker':'PIERAKSTS TIEŠSAISTĒ','booking.title':'Tavs krēsls <em>gaida.</em>','booking.copy':'Ievadi savus datus, izvēlies pakalpojumu un datumu. Sistēma rezervēs pirmo piemēroto laiku šīs dienas grafikā.','booking.notify':'Ieslēgt paziņojumus par pierakstu','form.name':'Vārds un uzvārds','form.age':'Vecums','form.service':'Pakalpojums','form.date':'Vēlamais datums','form.submit':'Nosūtīt pieteikumu','form.note':'Laiks nav jāizvēlas: pieteikumam tiks piešķirts pirmais brīvais laiks. Pēc tam administrators to apstiprinās.','form.open':'Šajā dienā strādājam no {open} līdz {close}. Tev tiks piešķirts tuvākais brīvais laiks.','form.closed':'Šajā dienā salons nestrādā. Izvēlies citu datumu.','form.booked':'Pieteikums saņemts: {date} plkst. {time}. Administrators drīz apstiprinās pierakstu.','form.noSlot':'Šajā dienā brīvu laiku vairs nav. Izvēlies citu datumu.','form.error':'Pieteikumu neizdevās nosūtīt. Pārbaudi datus un mēģini vēlreiz.','form.notifyOn':'Paziņojumi ir ieslēgti.','form.notifyOff':'Paziņojumi nav pieejami vai nav atļauti šajā pārlūkā.','form.available':'Brīvs','form.full':'Nav vietu','form.loading':'Pārbaude',
    'visit.kicker':'ATRODI KOTANS','visit.title':'Nāc iekšā,<br /><em>ieņem vietu.</em>','visit.addressLabel':'Adrese','visit.phoneLabel':'Tālrunis','visit.hoursLabel':'Darba laiks','visit.hours':'Pieejams<br />tiešsaistes grafikā','footer.copy':'Kvalificēti bārddziņi. Pārdomāta kopšana. Rēzekne.','footer.admin':'Administrators',
    'admin.kicker':'SLĒGTA ZONA','admin.loginTitle':'Administratora pieslēgšanās','admin.password':'Parole','admin.enter':'Ienākt','admin.title':'Pieteikumi','admin.notifications':'Ieslēgt paziņojumus','admin.logout':'Izrakstīties','admin.scheduleKicker':'DARBA LAIKS','admin.scheduleTitle':'Nedēļas grafiks','admin.scheduleCopy':'Apmeklētājiem tiks piešķirts tuvākais laiks šajās darba stundās. Atzīmē dienu kā brīvu, ja nestrādā.','admin.saveHours':'Saglabāt grafiku','admin.blockedKicker':'PIEKĻUVES KONTROLE','admin.blockedTitle':'Bloķētās IP adreses','admin.loginError':'Nepareiza parole.','admin.loginGeneric':'Neizdevās pieslēgties. Mēģini vēlreiz.','admin.empty':'Pieteikumu pagaidām nav.','admin.pending':'gaida lēmumu','admin.accepted':'apstiprināts','admin.rejected':'noraidīts','admin.accept':'Apstiprināt','admin.reject':'Noraidīt','admin.delete':'Dzēst','admin.block':'Bloķēt IP','admin.unblock':'Atbloķēt','admin.reason':'Ieraksti klientam noraidījuma iemeslu:','admin.deleteConfirm':'Vai neatgriezeniski dzēst šo pieteikumu? Tas pazudīs gan no vietnes, gan no Google Sheets.','admin.blockConfirm':'Vai bloķēt šo IP adresi? Jauni pieteikumi no tās vairs netiks pieņemti.','admin.hoursSaved':'Grafiks ir saglabāts.','admin.actionError':'Darbību neizdevās izpildīt. Atsvaidzini lapu un mēģini vēlreiz.','admin.newCount':'Kopā: {all} · gaida: {pending}','admin.notificationsOn':'Administratora paziņojumi ir ieslēgti.','admin.day0':'Svētdiena','admin.day1':'Pirmdiena','admin.day2':'Otrdiena','admin.day3':'Trešdiena','admin.day4':'Ceturtdiena','admin.day5':'Piektdiena','admin.day6':'Sestdiena','admin.open':'Sākums','admin.close':'Beigas','admin.closed':'Brīvdiena','admin.reasonNeeded':'Iemesls ir obligāts.','theme.light':'Gaiša','theme.dark':'Tumša'
  },
  en: {
    'nav.services':'Services','nav.work':'Work','nav.visit':'Visit','nav.book':'Book a visit',
    'hero.kicker':'BARBER SHOP · RĒZEKNE','hero.title':'A sharp cut.<br /><em>Time set aside</em> for you.','hero.lead':'A considered cut, an attentive barber and a space where you can take your time. Visits are by appointment.','hero.cta':'Make an appointment <span>↘</span>','hero.about':'About the shop <span>↓</span>','hero.caption1':'Dārzu iela 22','hero.caption2':'By appointment','hero.scroll':'SCROLL TO EXPLORE',
    'about.kicker':'THE KOTANS APPROACH','about.title':'Good grooming is a <em>quiet confidence.</em>','about.copy':'Kotans is made for people who value a precise result and a relaxed atmosphere. We take time to understand your hair, beard and routine before the work begins.','about.copy2':'Every barber on our team is professionally qualified. Technique, hygiene and a genuine, individual approach are non-negotiable in every chair.','facts.one':'Personal<br />consultation','facts.two':'30-minute<br />booking rhythm','facts.three':'Rēzekne,<br />Latvia',
    'services.kicker':'SERVICES','services.title':'Designed around <em>your look.</em>','services.note':'Choose a service and a preferred date. We will assign the earliest available time automatically.','services.consult':'consultation','services.ask':'ask your barber','service.haircut.name':'Haircut','service.haircut.text':'A precise, tailored cut with a brief consultation before we begin.','service.beard.name':'Beard trim','service.beard.text':'Shape, clean lines and care that works with your natural growth.','service.combo.name':'Haircut + beard','service.combo.text':'A complete appointment for a coherent, well-finished look.','service.hairTone.name':'Hair toning','service.hairTone.text':'Toning is planned individually — speak with your barber about the result and price.','service.beardTone.name':'Beard toning','service.beardTone.text':'A natural-looking refresh, agreed with your barber before the visit.',
    'work.kicker':'IN THE CHAIR','work.title':'The work speaks <em>for itself.</em>','work.one':'Detail in every movement.','work.two':'A finish made for you.',
    'booking.kicker':'ONLINE BOOKING','booking.title':'Your chair is <em>waiting.</em>','booking.copy':'Tell us who you are, choose your service and date. The system reserves the first suitable appointment in that day’s schedule.','booking.notify':'Turn on appointment notifications','form.name':'Name and surname','form.age':'Age','form.service':'Service','form.date':'Preferred date','form.submit':'Request an appointment','form.note':'You do not select a time: the first available time will be assigned to your request. The administrator will then confirm it.','form.open':'We are working from {open} to {close} on this date. You will receive the earliest suitable time.','form.closed':'The shop is closed on this date. Please choose another day.','form.booked':'Request received: {date} at {time}. The administrator will confirm your appointment shortly.','form.noSlot':'There are no appointment times left on this date. Please choose another day.','form.error':'We could not send your request. Check your details and try again.','form.notifyOn':'Notifications are on.','form.notifyOff':'Notifications are unavailable or not allowed in this browser.','form.available':'Available','form.full':'Full','form.loading':'Checking',
    'visit.kicker':'VISIT KOTANS','visit.title':'Come take a<br /><em>seat.</em>','visit.addressLabel':'Address','visit.phoneLabel':'Phone','visit.hoursLabel':'Hours','visit.hours':'Set by appointment<br />in the online schedule','footer.copy':'Qualified barbers. Considered grooming. Rēzekne.','footer.admin':'Administrator',
    'admin.kicker':'PRIVATE AREA','admin.loginTitle':'Administrator sign in','admin.password':'Password','admin.enter':'Enter','admin.title':'Appointments','admin.notifications':'Enable notifications','admin.logout':'Sign out','admin.scheduleKicker':'WORKING HOURS','admin.scheduleTitle':'Weekly schedule','admin.scheduleCopy':'Visitors receive the first suitable slot within these hours. Set a day to closed if you are not working.','admin.saveHours':'Save schedule','admin.blockedKicker':'ACCESS CONTROL','admin.blockedTitle':'Blocked IP addresses','admin.loginError':'Incorrect password.','admin.loginGeneric':'Could not sign in. Please try again.','admin.empty':'There are no requests yet.','admin.pending':'awaiting decision','admin.accepted':'confirmed','admin.rejected':'declined','admin.accept':'Accept','admin.reject':'Decline','admin.delete':'Delete','admin.block':'Block IP','admin.unblock':'Unblock','admin.reason':'Write the reason for the client:','admin.deleteConfirm':'Delete this request permanently? It will disappear from both the website and Google Sheets.','admin.blockConfirm':'Block this IP address? New requests from it will no longer be accepted.','admin.hoursSaved':'Schedule saved.','admin.actionError':'We could not complete this action. Refresh and try again.','admin.newCount':'Total: {all} · awaiting: {pending}','admin.notificationsOn':'Administrator notifications are enabled.','admin.day0':'Sunday','admin.day1':'Monday','admin.day2':'Tuesday','admin.day3':'Wednesday','admin.day4':'Thursday','admin.day5':'Friday','admin.day6':'Saturday','admin.open':'Start','admin.close':'End','admin.closed':'Closed','admin.reasonNeeded':'A reason is required.','theme.light':'Light','theme.dark':'Dark'
  }
};

let language = localStorage.getItem('kotans-language') || 'ru';
let appConfig = { pushPublicKey: '' };
let adminData = { bookings: [], hours: {}, blockedIps: [] };
let weekAvailability = {};
let weekAvailabilityRequest = 0;
const $ = (selector, node = document) => node.querySelector(selector);
const $$ = (selector, node = document) => [...node.querySelectorAll(selector)];
const t = (key, values = {}) => (copy[language][key] || copy.en[key] || key).replace(/\{(\w+)\}/g, (_, name) => values[name] ?? '');
const escape = (value) => String(value ?? '').replace(/[&<>'"]/g, (character) => ({ '&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;' }[character]));

function currentTheme() { return document.documentElement.dataset.theme === 'dark' ? 'dark' : 'light'; }
function updateThemeControl() {
  const theme = currentTheme();
  const button = $('#themeToggle');
  if (!button) return;
  const nextTheme = theme === 'dark' ? 'light' : 'dark';
  $('.theme-label', button).textContent = t(`theme.${nextTheme}`);
  button.setAttribute('aria-label', t(`theme.${nextTheme}`));
  button.setAttribute('aria-pressed', String(theme === 'dark'));
  document.querySelector('meta[name="theme-color"]').setAttribute('content', theme === 'dark' ? '#0c0b0e' : '#f2f1ed');
}
function toggleTheme() {
  const theme = currentTheme() === 'dark' ? 'light' : 'dark';
  document.documentElement.dataset.theme = theme;
  localStorage.setItem('kotans-theme', theme);
  updateThemeControl();
}

function todayLocal() {
  const now = new Date();
  const offset = now.getTimezoneOffset() * 60000;
  return new Date(now - offset).toISOString().slice(0, 10);
}
function dateFromIso(date) { return new Date(`${date}T12:00:00`); }
function isoFromDate(date) {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}
function nextSevenDays() {
  const first = dateFromIso(todayLocal());
  return Array.from({ length: 7 }, (_, offset) => {
    const date = new Date(first);
    date.setDate(first.getDate() + offset);
    return isoFromDate(date);
  });
}
function languageLocale() { return language === 'ru' ? 'ru-RU' : language === 'lv' ? 'lv-LV' : 'en-GB'; }
function weekStateLabel(state) {
  if (!state || state.loading) return t('form.loading');
  if (state.error) return '—';
  if (state.closed) return t('admin.closed');
  return state.available ? t('form.available') : t('form.full');
}
function renderWeekPicker() {
  const picker = $('#weekPicker');
  if (!picker) return;
  const selected = $('#bookingDate').value;
  const weekday = new Intl.DateTimeFormat(languageLocale(), { weekday: 'short' });
  picker.innerHTML = nextSevenDays().map((date) => {
    const parsed = dateFromIso(date);
    const state = weekAvailability[date];
    const disabled = !state || state.loading || state.error || state.closed || !state.available;
    const isSelected = selected === date && !disabled;
    return `<button type="button" class="week-day${isSelected ? ' selected' : ''}" data-date="${date}" aria-pressed="${isSelected}" ${disabled ? 'disabled' : ''}><span class="week-day-name">${escape(weekday.format(parsed).replace('.', ''))}</span><span class="week-day-date">${parsed.getDate()}</span><span class="week-day-state">${escape(weekStateLabel(state))}</span></button>`;
  }).join('');
}
function showSelectedAvailability() {
  const hint = $('#availabilityHint');
  const selected = $('#bookingDate').value;
  const state = weekAvailability[selected];
  hint.classList.remove('closed');
  if (!selected || !state) { hint.textContent = ''; return; }
  if (state.closed) { hint.textContent = t('form.closed'); hint.classList.add('closed'); return; }
  if (!state.available) { hint.textContent = t('form.noSlot'); hint.classList.add('closed'); return; }
  hint.textContent = t('form.open', state.hours);
}
async function refreshWeekAvailability() {
  const requestId = ++weekAvailabilityRequest;
  const dates = nextSevenDays();
  const service = $('#bookingForm [name="service"]').value;
  weekAvailability = Object.fromEntries(dates.map((date) => [date, { loading: true }]));
  renderWeekPicker();

  const results = await Promise.all(dates.map(async (date) => {
    try {
      return await api(`/api/availability?date=${encodeURIComponent(date)}&service=${encodeURIComponent(service)}`);
    } catch {
      return { error: true, available: false };
    }
  }));
  if (requestId !== weekAvailabilityRequest) return;

  weekAvailability = Object.fromEntries(dates.map((date, index) => [date, results[index]]));
  const selected = $('#bookingDate').value;
  const selectedState = weekAvailability[selected];
  if (!selectedState || selectedState.closed || !selectedState.available || selectedState.error) {
    const firstAvailable = dates.find((date) => weekAvailability[date]?.available && !weekAvailability[date]?.closed);
    $('#bookingDate').value = firstAvailable || '';
  }
  renderWeekPicker();
  showSelectedAvailability();
}
function applyLanguage() {
  document.documentElement.lang = language;
  $$('[data-i18n]').forEach((node) => { node.textContent = t(node.dataset.i18n); });
  $$('[data-i18n-html]').forEach((node) => { node.innerHTML = t(node.dataset.i18nHtml); });
  $$('.lang').forEach((button) => button.classList.toggle('active', button.dataset.lang === language));
  $$('.card-book').forEach((button) => button.setAttribute('aria-label', `${t('nav.book')}: ${t(`service.${button.dataset.service}.name`)}`));
  $('#weekPicker')?.setAttribute('aria-label', t('form.date'));
  updateThemeControl();
  localStorage.setItem('kotans-language', language);
  if (!$('#adminPanel').hidden) renderAdmin();
  if (Object.keys(weekAvailability).length) { renderWeekPicker(); showSelectedAvailability(); }
  else refreshWeekAvailability();
}
async function api(url, options = {}) {
  const response = await fetch(url, { headers: { 'Content-Type': 'application/json', ...(options.headers || {}) }, ...options });
  let body = {};
  try { body = await response.json(); } catch { /* empty reply */ }
  if (!response.ok) throw Object.assign(new Error(body.error || 'request_failed'), { code: body.error, status: response.status });
  return body;
}
function message(node, content, isError = false) { node.textContent = content; node.classList.toggle('error', isError); }
async function requestNotificationPermission(ask = false) {
  if (!('Notification' in window)) return false;
  if (ask && Notification.permission === 'default') return (await Notification.requestPermission()) === 'granted';
  return Notification.permission === 'granted';
}
async function updateAvailability() {
  const date = $('#bookingDate').value;
  const hint = $('#availabilityHint');
  hint.textContent = '';
  hint.classList.remove('closed');
  if (!date) return;
  try {
    const service = $('#bookingForm [name="service"]').value;
    const data = await api(`/api/availability?date=${encodeURIComponent(date)}&service=${encodeURIComponent(service)}`);
    if (data.closed) { hint.textContent = t('form.closed'); hint.classList.add('closed'); }
    else if (!data.available) { hint.textContent = t('form.noSlot'); hint.classList.add('closed'); }
    else hint.textContent = t('form.open', data.hours);
  } catch { hint.textContent = t('form.error'); hint.classList.add('closed'); }
}
async function enableClientNotifications() {
  const allowed = await requestNotificationPermission(true);
  message($('#bookingMessage'), allowed ? t('form.notifyOn') : t('form.notifyOff'), !allowed);
  return allowed;
}
async function submitBooking(event) {
  event.preventDefault();
  const form = event.currentTarget;
  const submit = $('.submit-booking', form);
  const data = Object.fromEntries(new FormData(form));
  if (!form.reportValidity()) return;
  if (!data.date) { message($('#bookingMessage'), t('form.noSlot'), true); return; }
  submit.disabled = true;
  message($('#bookingMessage'), '');
  try {
    await requestNotificationPermission(true);
    const result = await api('/api/bookings', { method: 'POST', body: JSON.stringify({ ...data, locale: language }) });
    message($('#bookingMessage'), t('form.booked', result.booking));
    form.reset();
    $('#bookingDate').value = '';
    await refreshWeekAvailability();
  } catch (error) {
    message($('#bookingMessage'), error.code === 'no_slot' ? t('form.noSlot') : error.code === 'closed' ? t('form.closed') : t('form.error'), true);
  } finally { submit.disabled = false; }
}

function showAdmin() { $('#adminDialog').showModal(); document.body.classList.add('dialog-open'); checkAdmin(); }
function hideAdmin() { $('#adminDialog').close(); document.body.classList.remove('dialog-open'); }
async function checkAdmin() {
  try {
    const me = await api('/api/admin/me');
    $('#adminLogin').hidden = me.isAdmin;
    $('#adminPanel').hidden = !me.isAdmin;
    if (me.isAdmin) await loadAdmin();
  } catch { /* no session */ }
}
async function login(event) {
  event.preventDefault();
  const form = event.currentTarget;
  const password = new FormData(form).get('password');
  try {
    await api('/api/admin/login', { method: 'POST', body: JSON.stringify({ password }) });
    $('#loginMessage').textContent = '';
    $('#adminLogin').hidden = true;
    $('#adminPanel').hidden = false;
    form.reset();
    await loadAdmin();
  } catch (error) { message($('#loginMessage'), error.code === 'wrong_password' ? t('admin.loginError') : t('admin.loginGeneric'), true); }
}
async function loadAdmin() {
  try {
    const [bookingData, settingsData, blockedData] = await Promise.all([api('/api/admin/bookings'), api('/api/admin/settings'), api('/api/admin/blocked-ips')]);
    adminData = { bookings: bookingData.bookings, hours: settingsData.hours, blockedIps: blockedData.blockedIps };
    renderAdmin();
  } catch (error) {
    if (error.status === 401) { $('#adminLogin').hidden = false; $('#adminPanel').hidden = true; }
  }
}
function renderAdmin() {
  const { bookings, hours, blockedIps } = adminData;
  const pending = bookings.filter((booking) => booking.status === 'pending').length;
  $('#adminSummary').textContent = t('admin.newCount', { all: bookings.length, pending });
  $('#adminBookings').innerHTML = bookings.length ? bookings.map((booking) => {
    const safeId = escape(booking.id);
    const controls = `<div class="booking-controls">${booking.status === 'pending' ? `<button class="accept" data-action="accept" data-id="${safeId}">${t('admin.accept')}</button><button class="reject" data-action="reject" data-id="${safeId}">${t('admin.reject')}</button>` : ''}<button data-action="delete" data-id="${safeId}">${t('admin.delete')}</button><button data-action="block" data-ip="${escape(booking.ip)}">${t('admin.block')}</button></div>`;
    return `<article class="admin-booking ${escape(booking.status)}"><div><h4>${escape(booking.name)}</h4><p>${escape(t(`service.${booking.service}.name`))} · ${escape(booking.age)} ${language === 'ru' ? 'лет' : language === 'lv' ? 'g.' : 'years'}</p><p class="booking-status">${escape(t(`admin.${booking.status}`))}</p></div><div><p><strong>${escape(booking.date)} · ${escape(booking.time)}–${escape(booking.endTime)}</strong></p><p>${escape(booking.price)} · IP ${escape(booking.ip)}</p>${booking.reason ? `<p>${escape(booking.reason)}</p>` : ''}</div>${controls}</article>`;
  }).join('') : `<p>${t('admin.empty')}</p>`;
  $('#hoursForm').innerHTML = Array.from({ length: 7 }, (_, day) => {
    const range = hours[day];
    return `<div class="day-hours" data-day="${day}"><span>${t(`admin.day${day}`)}</span><label>${t('admin.open')}<input class="open" type="time" value="${range?.open || '09:00'}" ${range ? '' : 'disabled'} /></label><label>${t('admin.close')}<input class="close" type="time" value="${range?.close || '18:00'}" ${range ? '' : 'disabled'} /></label><label class="closed-toggle"><input class="closed" type="checkbox" ${range ? '' : 'checked'} />${t('admin.closed')}</label></div>`;
  }).join('');
  $('#blockedIps').innerHTML = blockedIps.length ? blockedIps.map((item) => `<div class="blocked-ip"><span>${escape(item.ip)}</span><button data-unblock="${escape(item.ip)}">${t('admin.unblock')}</button></div>`).join('') : '<p>—</p>';
}
async function bookingAction(event) {
  const button = event.target.closest('button[data-action]');
  if (!button) return;
  const { action, id, ip } = button.dataset;
  try {
    if (action === 'accept') await api(`/api/admin/bookings/${encodeURIComponent(id)}`, { method: 'PATCH', body: JSON.stringify({ action }) });
    if (action === 'reject') {
      const reason = window.prompt(t('admin.reason'), '');
      if (reason === null) return;
      if (!reason.trim()) { window.alert(t('admin.reasonNeeded')); return; }
      await api(`/api/admin/bookings/${encodeURIComponent(id)}`, { method: 'PATCH', body: JSON.stringify({ action, reason }) });
    }
    if (action === 'delete') { if (!window.confirm(t('admin.deleteConfirm'))) return; await api(`/api/admin/bookings/${encodeURIComponent(id)}`, { method: 'DELETE' }); }
    if (action === 'block') { if (!window.confirm(t('admin.blockConfirm'))) return; await api('/api/admin/blocked-ips', { method: 'POST', body: JSON.stringify({ ip }) }); }
    await loadAdmin();
  } catch { window.alert(t('admin.actionError')); }
}
async function saveHours(event) {
  event.preventDefault();
  const hours = {};
  $$('.day-hours', event.currentTarget).forEach((row) => {
    const closed = $('.closed', row).checked;
    hours[row.dataset.day] = closed ? null : { open: $('.open', row).value, close: $('.close', row).value };
  });
  try { const result = await api('/api/admin/settings', { method: 'PUT', body: JSON.stringify({ hours }) }); adminData.hours = result.hours; renderAdmin(); await refreshWeekAvailability(); window.alert(t('admin.hoursSaved')); } catch { window.alert(t('admin.actionError')); }
}
async function toggleDayInputs(event) {
  if (!event.target.matches('.closed')) return;
  const row = event.target.closest('.day-hours');
  $$('.open, .close', row).forEach((input) => { input.disabled = event.target.checked; });
}
async function unblock(event) {
  const button = event.target.closest('[data-unblock]');
  if (!button) return;
  try { await api(`/api/admin/blocked-ips/${encodeURIComponent(button.dataset.unblock)}`, { method: 'DELETE' }); await loadAdmin(); } catch { window.alert(t('admin.actionError')); }
}
async function enableAdminNotifications() {
  try {
    const allowed = await requestNotificationPermission(true);
    if (!allowed) return window.alert(t('form.notifyOff'));
    window.alert(t('admin.notificationsOn'));
  } catch { window.alert(t('form.notifyOff')); }
}

document.addEventListener('DOMContentLoaded', async () => {
  $('#year').textContent = new Date().getFullYear();
  applyLanguage();
  try { appConfig = await api('/api/config'); } catch { /* booking works without push */ }
  $('#themeToggle').addEventListener('click', toggleTheme);
  $$('.lang').forEach((button) => button.addEventListener('click', () => { language = button.dataset.lang; applyLanguage(); }));
  $('#weekPicker').addEventListener('click', (event) => {
    const button = event.target.closest('.week-day:not(:disabled)');
    if (!button) return;
    $('#bookingDate').value = button.dataset.date;
    renderWeekPicker();
    showSelectedAvailability();
  });
  $('#bookingForm [name="service"]').addEventListener('change', refreshWeekAvailability);
  $$('.card-book').forEach((button) => button.addEventListener('click', async () => {
    $('#bookingForm [name="service"]').value = button.dataset.service;
    $('#booking').scrollIntoView({ behavior: 'smooth', block: 'start' });
    await refreshWeekAvailability();
  }));
  $('#bookingForm').addEventListener('submit', submitBooking);
  $('#notificationsButton').addEventListener('click', enableClientNotifications);
  $('#openAdmin').addEventListener('click', showAdmin);
  $('#closeAdmin').addEventListener('click', hideAdmin);
  $('#adminDialog').addEventListener('close', () => document.body.classList.remove('dialog-open'));
  $('#adminDialog').addEventListener('click', (event) => { if (event.target === $('#adminDialog')) hideAdmin(); });
  $('#loginForm').addEventListener('submit', login);
  $('#logout').addEventListener('click', async () => { await api('/api/admin/logout', { method: 'POST' }); $('#adminPanel').hidden = true; $('#adminLogin').hidden = false; });
  $('#adminBookings').addEventListener('click', bookingAction);
  $('#hoursForm').addEventListener('submit', saveHours);
  $('#hoursForm').addEventListener('change', toggleDayInputs);
  $('#blockedIps').addEventListener('click', unblock);
  $('#adminNotifications').addEventListener('click', enableAdminNotifications);
});
